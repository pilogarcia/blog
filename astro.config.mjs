// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // Reemplazá por tu dominio real (necesario para el RSS y las URLs canónicas).
  site: 'https://blog.elojoinusual.com.ar',
});
